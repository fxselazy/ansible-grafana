---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  ansible-lint ../../etc
platforms:
  - name: ubuntu2004-grafana
    image: quay.io/ansible/ubuntu2004-test-container
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    capabilities:
      - SYS_ADMIN
    command: "/lib/systemd/systemd"
    exposed_ports:
     - 3000/tcp
    published_ports:
     - 0.0.0.0:3000:3000/tcp
    privilaged: true  
    pre_build_image: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint
    options:
      exclude:
        - meta/
  playbook:
    create: create.yml
    prepare: prepare.yml
    converge: converge.yml
    destroy: destroy.yml
scenario:
  name: default
verifier:
  name: ansible